[project]
name = "proyectodfr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "IvanEscobar", email = "ivanescobar.tfr@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "djangorestframework>=3.18.0",
]

[project.scripts]
proyectodfr = "proyectodfr:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
